import UIKit

class ViewController: UIViewController {
    
    // TODO: - In Main.storyboard; click the Library icon and drag varying UI elements onto the ViewController scene. Select each element added and open the Attributes inspector for that element.

    override func viewDidLoad() {
        super.viewDidLoad()
    }
}
